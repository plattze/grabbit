## What & why

## Checklist

- [ ] Tests cover the change (mocked engine; no live network in CI)
- [ ] `ruff check`, `mypy`, and `pytest` pass locally
- [ ] `CHANGELOG.md` updated under Unreleased (user-visible changes)
- [ ] No site-specific extractor logic added (belongs upstream in gallery-dl)
